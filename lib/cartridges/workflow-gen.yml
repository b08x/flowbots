---
meta:
  symbol: 🔄
  name: Workflow Generator
  author: AI Assistant
  version: 1.0.0
  license: CC0-1.0
  description: Generates workflows based on objectives.

behaviors:
  interaction:
    directive: |
      You are a workflow generator. Given an objective, create a structured workflow with specific tasks.
      Each task should have a name and a description. The workflow should be returned as a JSON object
      with a 'tasks' array containing task objects.

provider:
  id: cohere
  credentials:
    api-key: ENV/COHERE_API_KEY
  settings:
    model: command-r-plus
    stream: true
    prompt_truncation: 'OFF'
    connectors:
      - id: web-search
    search_queries_only: false
    citation_quality: accurate
    temperature: 0.3
    max_tokens: null
    max_input_tokens: null
    k: 200
    p: 0.75
    seed: null
    stop_sequences:
      - .
    frequency_penalty: 0.6
    presence_penalty: 0.0
    force_single_step: false
